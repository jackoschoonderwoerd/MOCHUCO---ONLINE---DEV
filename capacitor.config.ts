import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.itudiant',
  appName: 'ITudiant',
  webDir: 'dist/Mochuco',
  bundledWebRuntime: false
};

export default config;
